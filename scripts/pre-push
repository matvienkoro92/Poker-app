#!/usr/bin/env bash
# Git hook intentionally does not bump app versions automatically.
# Run npm run bump:pwa-version manually only when a release needs a cache-bust.
set -euo pipefail

npm run release:check
