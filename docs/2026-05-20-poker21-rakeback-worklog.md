# Poker21 profile and admin rakeback worklog - 2026-05-20

Этот документ фиксирует закрытый слой правок от 20 мая 2026: новую статистику Poker21 в профиле, точные периоды по датам, стабилизацию рейкбек-таблицы, шаблоны, итоги и отправленные отчеты.

Это уже сделанная работа, а не список задач.

## Коротко

- В профиль Poker21 выведены новые поля статистики из `today_counter`, `week_counter`, `total_counter`.
- Нулевые значения статистики больше не прячутся.
- `mtt_round` и `sng_round` убраны из профиля, потому что это не то же самое, что количество сыгранных турниров.
- Для статистики Poker21 сделаны вкладки `Сегодня`, `Неделя`, `Всего`, `Период`.
- `Период` открывает один календарь, где можно выбрать диапазон от одной даты до другой.
- Календарь Poker21 показывает кликабельными только даты, по которым у нас есть точный дневной снимок статистики.
- Для рейкбека добавлены цвета строк, копирование ID, строгие проверки сохранения, подтверждение удаления и устойчивое поведение шаблонов.
- Итоги рейкбека снова открываются по клику и показывают `рейк / рейкбек`, в том числе по датам.
- В отправленных отчетах ускорено первое открытие и добавлены новые итоговые блоки.

## Poker21 stats in profile

Poker21 передает счетчики:

- `total_counter`;
- `today_counter`;
- `week_counter`.

Backend нормализует эти счетчики в `lib/pokerplus.js`, frontend рисует их в `app-profile-pokerplus.js`.

В профиле показываются:

- `fee` как `Рейк`;
- `hands` как `Хендс`;
- `winnings` как `Кеш`;
- `mtt_winnings` как `MTT`;
- `mtt_count` как `MTT игр`;
- `mtt_itm_count` как `MTT ITM`;
- `mtt_1st_count` как `MTT 1-е`;
- `sng_winnings` как `SNG`;
- `sng_count` как `SNG игр`;
- `sng_itm_count` как `SNG ITM`;
- `sng_1st_count` как `SNG 1-е`.

Нулевые значения выводятся явно. Это важно для полей вроде `mtt_count`, `mtt_itm_count`, `sng_count`, где `0` тоже является полезным ответом от Poker21.

Поля, которые нормализуются, но не выводятся в профиль:

- `mtt_round`;
- `sng_round`;
- `bb`;
- `ofc_winnings`.

`mtt_round` и `sng_round` убраны после уточнения, что пользователю нужны именно игры/турниры (`mtt_count`, `sng_count`), ITM и первые места, а не внутренние раунды.

## Poker21 period tabs and exact date ranges

Вместо трех больших блоков статистики сделаны четыре вкладки:

- `Сегодня`;
- `Неделя`;
- `Всего`;
- `Период`.

Вкладка `Период` открывает один календарь. Первый клик выбирает начало диапазона, второй клик выбирает конец. Если выбран один день, период считается как один день.

Календарь не дает выбрать даты без точных данных. Это сделано намеренно: если по дате нет дневного снимка, интерфейс не должен показывать "точную" статистику, которой на самом деле нет.

## Poker21 daily snapshots

Для точных периодов backend сохраняет дневные снимки Poker21-статистики в Redis:

```text
poker_app:pokerplus_profile_snapshots:<accountId>
```

Формат ключа описан в `docs/redis-schema.md`.

Каждая запись хранится как `YYYY-MM-DD -> JSON`. Дата снимка считается по Москве (`Europe/Moscow`) в `lib/pokerplus.js`.

Важное ограничение: точный период работает только с датами, которые уже были сохранены нашим приложением как дневные снимки. Если игрок подключился только сегодня, прошлые дни не появятся автоматически задним числом, если Poker21 не отдаст нам отдельную историческую дневную статистику.

Сегодняшний `today_counter` может участвовать в периоде как текущая доступная дата.

## Poker21 stats layout

Блок статистики в профиле был уплотнен:

- убраны лишние вложенные рамки вокруг `Показывать другим`;
- ряд вкладок стал плоским, без отдельной карточки вокруг кнопок;
- календарь периода стал компактнее;
- сообщение о видимости статистики стало строкой статуса, а не отдельной большой плашкой;
- вложенный импорт `styles-profile-poker21.css` обновлен до актуальной версии, чтобы браузер не держал старый CSS в кэше.

## Admin rakeback rows

Во вкладке `Рейкбек` закрыты следующие правила:

- клик по полю `Айди` копирует ID игрока;
- пустую запись нельзя сохранить;
- строку нельзя сохранить без процента;
- строку нельзя сохранить, если нет данных для расчета рейкбека;
- отрицательные значения запрещены во всех числовых полях;
- несохраненные строки не участвуют в итогах;
- при удалении строки показывается подтверждение;
- кнопки строк остаются рабочими во время фонового сохранения, чтобы интерфейс не выглядел зависшим;
- цвет строки сохраняется и не пропадает после сохранения/перерисовки;
- у шаблонов тоже есть кнопка выбора цвета.

Для фишечных румов в заголовках явно указано, что `Рейк` и `РБ` вводятся в фишках:

- `Хпокер`;
- `Супрема`;
- `PPpoker`.

При общем сравнении по всем румам фишечные значения по-прежнему пересчитываются через множители рума.

## Rakeback templates

Шаблонные строки получили отдельное поведение:

- если в шаблоне ввести только процент без рейка, это не создает рабочую запись и не поднимает шаблон наверх;
- такой процент сохраняется как дефолт для шаблона;
- после ввода процент сохраняется автоматически, без нажатия на галочку;
- сохраненное поле процента визуально становится "зафиксированным";
- цвет шаблона тоже может быть закреплен за шаблоном;
- повторный статус `Шаблоны загружены` больше не должен появляться при каждом сохранении;
- поиск по ID ищет и по скрытым шаблонам;
- скрытые шаблоны не рендерятся все сразу, чтобы не возвращать долгую загрузку.

Инвариант: поиск может раскрыть нужный шаблон, но закрытый список шаблонов не должен заранее рисовать все 165 строк.

## Rakeback addons and dates

Для связанных доп. строк рейкбека закреплены правила:

- доп. строка ставится сразу после строки/доп. строки, от которой она создана;
- цепочка доп. строк сохраняет порядок;
- расчет доп. строки идет от дельты к предыдущему рейку в группе;
- удаление защищено от ломания цепочки: нельзя удалить середину так, чтобы остались зависшие последующие доп. строки;
- дата записи отображается бейджем рядом с номером/ID;
- дата записи выбирается через календарный input;
- новая дата для рейкбек-записей переключается по cutoff `12:00 МСК`.

## Rakeback totals

Итоги рейкбека были восстановлены и расширены:

- строка `Итого по всем румам` снова кликабельна;
- по клику открывается модалка итогов;
- в модалке показываются итоги по румам;
- в модалке показываются итоги по датам;
- итоги по датам выводятся как `рейк / рейкбек`;
- несохраненные строки не попадают в итоги.

## Sent reports

Во вкладке `Отправленные` закрыты такие изменения:

- первое открытие ускорено через preload;
- выбранная вкладка визуально подсвечивается;
- добавлены месячные итоги отправленных отчетов;
- над `Итого Эксчип` добавлен блок `Итого Рунекс`;
- в `Итого Рунекс` выводится число Боткрипты;
- в деталях/итогах по датам показывается пара `рейк / рейкбек`.

## Detailed admin report session notes

Этот раздел фиксирует пользовательские решения, принятые в ходе серии быстрых правок вокруг вкладок `Рейкбек`, `Отправленные`, `Расчеты` и связанных отчетов. Его цель - оставить не только список коммитов, но и продуктовую логику, чтобы будущие фиксы не откатили важные договоренности.

### Loading and responsiveness

- Открытие админского отчета не должно блокировать клики на десятки секунд. Критичный shell отчета показывается сразу, тяжелые доменные модули догружаются после первого paint.
- Вкладка `Отправленные` при открытии тянет только `currentWeek`, а архив и месячные итоги загружаются лениво по раскрытию соответствующего спойлера.
- Быстрый HTML-cache текущей недели хранится в `localStorage`; при принудительном обновлении уже отрисованный контент не заменяется медленной пустой заглушкой.
- Архивные недели остаются lazy: пока пользователь не открыл `Прошлые недели`, запрос `scope=archive` не должен уходить.
- Месячные итоги тоже lazy: пока пользователь не открыл `Итого по месяцам`, запрос `scope=all` не должен уходить.
- Скрытые шаблоны рейкбека не должны рендериться все сразу. Поиск по ID может раскрыть нужный шаблон, но закрытая вкладка не должна создавать сотни строк DOM при старте.

### Shared rakeback draft

- Записи рейкбека создаются, редактируются, сохраняются и удаляются в общем серверном черновике.
- Все админы с доступом видят общий черновик после нажатия `Обновить`; автоматический realtime push между открытыми вкладками не вводился.
- Серверное сохранение должно быть merge-операцией, а не безусловным `SET`: строки других админов нельзя перетирать своим сохранением.
- Каждая строка хранит владельца (`ownerId`). Общая таблица видна всем, но в отправляемый отчет админа попадают только строки этого админа.
- Несохраненные и невалидные строки не участвуют ни в нижних итогах, ни в отправляемом отчете.
- Удаление строки подтверждается и не должно приводить к возврату удаленной записи после следующей загрузки shared draft.

### Template rows

- Шаблоны ID остаются редактируемыми прямо в таблице: ID, рейк, процент, `-15%`, цвет.
- Галочка у шаблона сохраняет заполненный шаблон как реальную запись и поднимает ее в рабочие записи наверх, фактически "выдергивая" запись из шаблона.
- Если в шаблоне введен только процент без рейка, это считается дефолтом шаблона, а не рабочей записью.
- Дефолтный процент шаблона должен сохраняться и визуально фиксироваться без создания нулевой записи.
- У шаблонов можно сохранять цвет; цвет не должен теряться после перерисовки.

### Addon rows

- Кнопка `+` на рабочей строке создает подзапись к этой строке.
- Если у строки уже есть подзаписи, новый `+` должен добавлять следующую подзапись после последней существующей и делать ее зависимой от предыдущей подзаписи, а не снова от базовой строки.
- Подзапись считает дельту рейка относительно предыдущей строки в группе. Например, если было `25000`, следующая подзапись `27000` дает дельту `2000`.
- Порядок строк после удаления не должен меняться: базовые строки и их цепочки остаются на своих местах.
- Нельзя удалить предыдущую подзапись, если после нее есть более новая подзапись. Сначала удаляется последняя строка цепочки.
- У подзаписей есть собственная отчетная дата. Это нужно для сценария, когда базовая запись добавлена в один день, а продолжение к ней - на следующий.
- Дата показывается бейджем рядом со строкой и редактируется через календарный input.

### Rakeback totals

- В нижнем итоге активного рума показывается пара `рейк / рейкбек`.
- Для фишечных румов `Хпокер`, `Супрема`, `PPpoker` значение рейка в итогах показывается в рублях с учетом множителя, а не сырыми фишками.
- `Итого по всем румам` суммирует все румы, а не только активную вкладку.
- В группах с подзаписями итоговый рейк по строке должен брать последнее значение цепочки, а не сумму базовой записи и подзаписей.
- Рейкбек при этом считается по сохраненным строкам и их дельтам, чтобы итог не удваивался.

### Sent reports monthly totals

- В `Отправленные` добавлен визуально отдельный цветной спойлер `Итого по месяцам`.
- Внутри каждого месяца есть `Итого за месяц` - агрегат всех отчетов месяца.
- Под ним есть `Общий отчёт по дням`: каждый день раскрывается отдельно и показывает суммарные поля за этот день.
- Еще ниже есть `Недельные итоги месяца`, чтобы видеть разрез месяца по неделям.
- Этот блок намеренно грузится по `scope=all` только при открытии, чтобы не ломать быстрое открытие текущей недели.

## Backend and storage

Задетые backend-слои:

- `lib/pokerplus.js` - нормализация новых Poker21 counter-полей и дневные снимки;
- `lib/api-handlers/admin-report-shifts.js` - нормализация рейкбек-строк, дат, цветов и сохранения shared draft;
- `docs/redis-schema.md` - добавлен ключ дневных снимков Poker21.

Задетые frontend-слои:

- `app-profile-pokerplus.js`;
- `styles-profile-poker21.css`;
- `styles-profile.css`;
- `html-fragments/profile.html`;
- `app-admin-reports-rakeback.js`;
- `app-admin-reports-sent.js`;
- `app-admin-reports-tabs.js`;
- `app-admin-reports.js`;
- `html-fragments/global-modals-admin.html`;
- `styles-admin.css`.

## Version and cache notes

В течение блока версия PWA поднималась на каждом фикс-пуше. После UI-уплотнения Poker21 stats версия была `3.110`.

Отдельно важно: `styles.css` обновлял основной cache-bust, но вложенный `@import` внутри `styles-profile.css` все еще ссылался на старый `styles-profile-poker21.css?v=3.074`. Это было исправлено отдельным коммитом, чтобы пользователи не получали старую версию CSS из кэша.

## Commit map

- `e4d4df9c Show rakeback rake totals in rubles`
- `da0118f0 Show zero Poker21 stats in profile`
- `56f9ba51 Fix rakeback addon deletion`
- `0ab4f65f Keep rakeback order on addon delete`
- `625793c7 Chain rakeback addon deltas`
- `988d191d Show tournament stats in Poker21 profile`
- `91c6a447 Place new rakeback addons after prior addon`
- `058144da Hide Poker21 round stats in profile`
- `df06fcf8 Require deleting latest rakeback addon first`
- `794a9aed fix: improve rakeback row controls`
- `a43fca20 fix: refine rakeback draft UX`
- `67227e94 fix: require rakeback percent before save`
- `70f0beb4 fix: add template row color control`
- `e7c7ec0a fix: block negative rakeback values`
- `e822e772 fix: label rakeback chip units by room`
- `b3294a72 fix: exclude unsaved rakeback rows from totals`
- `deb30d6a fix: preserve rakeback row colors`
- `8d5c6df3 fix: restore rakeback totals modal`
- `8eb7da36 fix: keep rakeback controls active while saving`
- `dccc619a fix: confirm rakeback row deletion`
- `70da90d2 fix: show rake in date totals`
- `518daed6 fix: require rakeback before saving rows`
- `937e5444 fix: preload sent reports tab`
- `535b7f41 fix: show runex total in sent reports`
- `34f6a325 fix: add poker21 stats snapshots and tabs`
- `4562f91d fix: add monthly sent report totals`
- `62b71c54 fix: keep poker21 date filters clickable`
- `835610b5 fix: move poker21 range picker into tabs`
- `dce110ee fix: compact poker21 stats controls`
- `10e9515c fix: refresh poker21 profile style import`

## Verification used during the block

Использовались проверки:

- `npm run check:syntax`;
- `npm run build`;
- `git diff --check`;
- `npm run smoke:nav` для затронутых навигационных сценариев;
- локальные browser-проверки для открытия приложения, профиля, админского отчета и консольных ошибок;
- contract tests после backend-изменений, где это было применимо.

## Future invariants

- Не возвращать `mtt_round` и `sng_round` в профиль как "игры".
- Не показывать `bb` и `ofc_winnings` в профиле без отдельного продуктового решения.
- В `Периоде` Poker21 выбирать можно только даты с точными снимками.
- Не рисовать скрытые шаблоны рейкбека все сразу.
- Не считать несохраненные/невалидные рейкбек-строки в итогах.
- Для `Хпокер`, `Супрема`, `PPpoker` не смешивать фишки с рублями без множителя.
- Подтверждение удаления строк рейкбека должно оставаться.
