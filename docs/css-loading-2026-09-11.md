# Отложенная загрузка CSS — 11 сентября 2026

Стартовый объём локальных CSS-ресурсов уменьшен с **881 575 до 781 938 байт**: минус **99 637 байт (11,3%)** после минификации, до сетевого сжатия. У двух изменённых стартовых файлов суммарный gzip уменьшился на **13 355 байт**. Это измерение объёма ресурсов, а не обещание такого же процента ускорения запуска; внешние шрифты в подсчёт не включены.

| Файл в сборке | До, байт | После, байт |
|---|---:|---:|
| styles.css | 557 836 | 514 080 |
| styles-home-overrides.css | 250 336 | 194 455 |

Правила отдельных экранов из базовых стилей, layout и styles.css перенесены в `styles-section-layout.css`. Его загружает существующий загрузчик при первом открытии соответствующего раздела. Оформление окон новостей клуба/друзей и их превью вынесено из home-overrides в `styles-home-news-modal.css`; оно загружается перед показом окна. Основные стили главной, навигации и заставок остались на старте.

Новые таблицы стилей вставляются в предусмотренную позицию исходного HTML, сохраняя порядок относительно остальных файлов. Все 3 156 CSS-правил затронутых исходников сохранены вместе с условиями media/supports; удаление старых правил на основании предположений не выполнялось. Обновлён статический кэш PWA: v61 → v62.

Проверено на локальной production-сборке:

- Сборка, синтаксис изменённого JS и всех 83 CSS-файлов — успешно.
- Визуальный smoke: 12 разных экранов, включая зал славы, и возврат на главную после загрузки разделов — успешно. Вычисленные стили до/после совпали, кроме текущего состояния анимаций (filter/transform/background-position).
- Новости: по 300 видимых элементов на ширине 390 и 1280 px — вычисленные стили полностью совпали; снимки проверены визуально.
- На главной оба новых CSS-файла не запрашиваются. Окно новостей остаётся скрытым до загрузки оформления. HTTP 503 показывает ошибку, повторное открытие успешно загружает CSS; следующие открытия используют уже загруженный файл.
- Навигационный smoke и PWA update — успешно; свежие стили появляются после явного обновления PWA.

Воспроизведение: `npm run build:static`, `npm run smoke:css`, `SMOKE_ROOT=public npm run smoke:visual`, `SMOKE_ROOT=public npm run smoke:nav`, `npm run test:pwa-update`. Сравнительные снимки и численные результаты текущей проверки находятся локально в `output/css-loading-2026-09-11/`.
